{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "autoinstall": "npm ci && lerna run autoinstall",
    "link": "lerna link",
    "build:guard": "cd packages/guard && npm run autoinstall && npm run build",
    "build:guard-shim-react18": "cd packages/guard-shim-react18 && npm run autoinstall && npm run build",
    "oss": "node scripts/oss.js",
    "lint": "eslint --ext .tsx,.ts packages/**/src/**",
    "lint:fix": "eslint --fix --ext .tsx,.ts packages/**/src/**"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16.13.1"
  },
  "author": "https://github.com/authing",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.32.0",
    "ali-oss": "^6.16.0",
    "eslint": "^8.7.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^8.0.1"
  }
}
