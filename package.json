{
  "name": "root",
  "private": true,
  "scripts": {
    "link": "lerna link",
    "build:guard": "cd packages/guard && npm run autoinstall && npm run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16.13.1"
  }
}
